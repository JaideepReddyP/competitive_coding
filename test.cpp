#include<bits/stdc++.h>
using namespace std;

int sum(string s) {
    int ans = 0;
    for(auto i : s) {
        ans += (i - '0');
    }
    return ans;
}

int main()
{
    // vector<string> s = {"hi", "ho", "ab", "ba"};
    // sort(s.begin(), s.end());
    // for(auto i : s) cout << i << ' ';
    int i = 1;
    !i;
    cout << -2 % 3;
} 