#include<bits/stdc++.h>
using namespace std;

int main() {
    int t;
    cin >> t;
    while(t--) {
        int n;
        cin >> n;
        vector<int> a(n);
        string s;
        for(int i = 0; i < n; i++) cin >> a[i];
        cin >> s;

        int q;
        cin >> q;
        while(q--) {
            int op;
            cin >> op;
            if(op == 1) {
                int l, r;
                cin >> l >> r;
                for(int i = l-1; i < r; i++) {
                    
                }
            }else if(op == 2) {

            }
        }
    }
}